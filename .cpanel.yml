---
# Deploy child theme lên cPanel (WordPress trong public_html)
# Tài khoản: /home/lajkpxfb
# Repo chỉ chứa OceanWP Child Theme — KHÔNG deploy vào gốc public_html.
deployment:
  tasks:
    - export DEPLOYPATH=/home/lajkpxfb/public_html/wp-content/themes/oceanwp-child-theme-master
    - /bin/mkdir -p "$DEPLOYPATH"
    - /bin/cp -Rf * "$DEPLOYPATH"
